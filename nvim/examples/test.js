window.addEventListener('eventName', event => {
  let magic = {
    foo: { bar: {} },
  }
  var bar=null
const FOO_BAR=1
  var foo = () => {
    event.preventDefault()

    return magic.foo
  }

  foo()
})
