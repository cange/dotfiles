return {
  log = {
    level = "info",
  },
  lsp = {
    diagnostic_virtual_text = true,
    format_on_save = true,
  },
  ui = {
    ---@type "none" | "single" | "double" | "rounded" | "shadow"
    border = "rounded",
  },
}
